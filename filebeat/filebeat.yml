filebeat.inputs:
        #- type: docker
        #containers.ids: '*'
        #processors:
        #- add_docker_metadata: ~
- type: container
  paths:
    - '/var/lib/docker/containers/*/*.log'


output.logstash:  
   hosts: ["localhost:5000"]

